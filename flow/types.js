// @flow
type Config = {
  blue: number,
  reds: Array<number>,
  selected: number,
  size: number,
  yellow: number
};

type CellConfig = {
  blue?: boolean,
  main?: boolean,
  red?: boolean,
  size: number,
  yellow?: boolean
};
