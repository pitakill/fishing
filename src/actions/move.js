// @flow
export const moveToLeft = (main: number): Action => ({type: 'MOVE_TO_LEFT', main});
export const moveToUp = (main: number): Action => ({type: 'MOVE_TO_UP', main});
export const moveToRight = (main: number): Action => ({type: 'MOVE_TO_RIGHT', main});
export const moveToDown = (main: number): Action => ({type: 'MOVE_TO_DOWN', main});
